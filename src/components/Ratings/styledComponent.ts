import styled from "styled-components";

export const RatingContainer = styled.div`
    display: flex;
    flex-flow: row nowrap;

    svg{
        fill: white;
        height: 12px;
        width: auto;
    }
`